const DOMAINS = ["example.com", "mail.io", "corp.net", "dev.org"];

export async function createEmail(state) {
  // const slug = state.user.name.toLowerCase().replace(/\s+/g, ".");
  // const domain = DOMAINS[Math.floor(Math.random() * DOMAINS.length)];

  return {
    restOfData: "HERE!!!!",
    blbla:1
  };
}
